
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestion-service
  namespace: contextengine
  labels:
    app.kubernetes.io/name: ingestion-service
    app.kubernetes.io/part-of: contextengine
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ingestion-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ingestion-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8082"
    spec:
      containers:
        - name: ingestion-service
          image: contextengine/ingestion-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: contextengine-config
            - secretRef:
                name: contextengine-secrets
          env:
            - name: SERVER_PORT
              value: "8082"
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 45
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: contextengine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ingestion-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
