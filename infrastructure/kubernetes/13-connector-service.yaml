
apiVersion: apps/v1
kind: Deployment
metadata:
  name: connector-service
  namespace: contextengine
  labels:
    app.kubernetes.io/name: connector-service
    app.kubernetes.io/part-of: contextengine
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: connector-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: connector-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8083"
    spec:
      containers:
        - name: connector-service
          image: contextengine/connector-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: contextengine-config
            - secretRef:
                name: contextengine-secrets
          env:
            - name: SERVER_PORT
              value: "8083"
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 45
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: connector-service
  namespace: contextengine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: connector-service
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
